¿El artefacto de salida se detiene explícitamente antes de aplicar el patch
(`aplicar_patch_memoria`) y pide confirmación humana, en vez de reportar el patch como ya
aplicado?

La creación inicial de un producto (Flujo A de `agents/documentador-klap.md`) siempre requiere
esa pausa, sin excepción — aprueba sólo si la salida deja claro que espera aprobación antes de
escribir a Klap Knowledge.
