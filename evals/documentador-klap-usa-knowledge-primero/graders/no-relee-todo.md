¿El artefacto de salida indica explícitamente que sólo se leyó/procesó la épica `SVA-2100` (la
única marcada como desalineada por `estado_fuentes`), y no relee ni reprocesa `SVA-1900` ni las
páginas de Confluence ya al día?

Reprueba si el agente reporta haber revisado todo el histórico del producto por defecto, en vez
de usar el delta que `estado_fuentes` ya le entregó.
