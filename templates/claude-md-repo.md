# {{nombre_componente}}

Componente del producto **{{nombre_producto}}**. Memoria técnica completa en
`component.yaml` y `docs/context/index.yaml` — léelos primero, siempre.

## Antes de trabajar en este repo

1. Lee `component.yaml` (identidad, dependencias, APIs, datos, eventos).
2. Lee `docs/context/index.yaml` y abre **sólo** los documentos que su `resumen` marque
   relevantes para la tarea actual — no el resto de `docs/`.
3. Si la tarea viene de una HU Jira, usa `/klap:trabajar-hu <ISSUE-KEY>` en vez de trabajar
   ad hoc.

## Reglas de este repo

<!-- Sólo lo que sea específico de ESTE componente y no esté ya en standards/ del Dev-Kit.
     No repitas estándares generales de Klap aquí — eso vive en standards/index.yaml. -->

- {{regla_especifica_1}}
- {{regla_especifica_2}}

No importar aquí documentación completa de `docs/` — este archivo debe mantenerse corto.
