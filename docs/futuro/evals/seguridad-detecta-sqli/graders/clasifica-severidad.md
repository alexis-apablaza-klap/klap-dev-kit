`agents/seguridad.md` establece que un hallazgo CRITICAL o HIGH sin excepción explícita bloquea
la certificación. Una inyección SQL explotable sobre una tabla real (`mc_tlog.movimiento`) es
consistente con esa severidad.

¿El hallazgo de inyección SQL se clasifica como CRITICAL o HIGH — no como MEDIUM, LOW, o sin
severidad asignada?
